verison: 0.2.5

preamble: >
  @init_plot_env();
  const id = "obstacle";
  const u = 0.01;
  const datadir = joinpath("data", id);
  const ni = 200;
  const nj = 50;
  const nu = 0.2;
  const constit_rel_f = init_constit_srt_const(nu);

datadir:    {   value:  datadir,    expr:   true  }

rho_0:      {   value:  1.0,        expr:   false }
nu:         {   value:  nu,         expr:   true  }

dx:         {   value:  1.0,        expr:   false }
dt:         {   value:  1.0,        expr:   false }
ni:         {   value:  ni,         expr:   true  }
nj:         {   value:  nj,         expr:   true  }

nsteps:     {   value: 100000,      expr:   false }

col_f:      init_col_srt(constit_rel_f);

sbounds:
  value: >
    tranpose([1    50    1     nj;
              50   65    1     14;
              50   65    36    nj;
              65   ni    1     nj]);
  expr: true

cbounds:
  value: >
    tranpose([1    50    1     nj;
              50   65    1     14;
              50   65    36    nj;
              65   ni    1     nj]);
  expr: true

# boundary conditions
bcs:
  - north_bounce_back!
  - south_bounce_back!
  - >
    begin;
      bind_west_inlet!(lat::Lattice) = west_velocity!(lat, u);
      return bind_west_inlet!;
    end
  - >
    begin;
      bind_east_pressure!(lat::Lattice) = east_pressure!(lat, 1.0);
      return bind_east_pressure!;
    end
  - >
    begin;
      obstacle_bounce_back!(lat::Lattice) = begin;
        north_bounce_back!(lat, 50, 65, 14);
        west_bounce_back!(lat, 50, 14, 36);
        south_bounce_back!(lat, 50, 65, 36);
        east_bounce_back!(lat, 65, 14, 36);
      end
      return obstacle_bounce_back!;
    end
# callback functions
callbacks:
  - plot_streamlines_callback(10, joinpath(datadir, "s1"), 0.0)
  - print_step_callback(100, id)
  - write_jld_file_callback(datadir, 500)

# clean-up, backup, write out
finally:
  - plot_streamlines_callback(1, joinpath(datadir, "sl"), 0.0);
  - write_jld_file_callback(datadir)
